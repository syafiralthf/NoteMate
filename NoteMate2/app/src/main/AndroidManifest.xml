<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin Internet untuk fitur Gemini AI -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Izin Mikrofon untuk fitur Voice Note (Speech-to-Text) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Izin Biometrik untuk fitur Scan Wajah / Sidik Jari (BARU) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteMate"
        tools:targetApi="31">

        <!-- Activity Awal (Splash Screen) -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity dengan Tema Material agar Chip & FAB tidak crash -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.NoteMate" />

        <activity
            android:name=".NoteActivity"
            android:exported="false"
            android:theme="@style/Theme.NoteMate"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>